<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fnd_drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <include
            android:id="@+id/fnd_toolbar"
            layout="@layout/layout_toolbar" />

        <!--We need android:clickable="true" because of this issue:
       http://stackoverflow.com/questions/17896052/why-does-drawerlayout-sometimes-glitch-upon-opening-->
        <FrameLayout
            android:id="@+id/fnd_content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/fnd_toolbar"
            android:clickable="true" />

    </RelativeLayout>

    <android.support.design.widget.NavigationView
        android:id="@+id/fnd_nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/layout_nav_header"
        app:menu="@menu/activity_navigation_drawer_drawer"
        app:theme="@style/NavigationMenuTheme" />

</android.support.v4.widget.DrawerLayout>
